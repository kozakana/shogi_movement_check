require 'spec_helper'

describe ShogiMovementCheck do
  it 'has a version number' do
    expect(ShogiMovementCheck::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
